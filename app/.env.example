# Optional OIDC Authorization Code + PKCE settings. These are public browser-client values.
# Keep every value empty until an identity provider has been selected and registered.
VITE_OIDC_AUTHORITY=
VITE_OIDC_CLIENT_ID=
VITE_OIDC_REDIRECT_URI=
VITE_OIDC_POST_LOGOUT_REDIRECT_URI=
VITE_OIDC_SCOPE=
