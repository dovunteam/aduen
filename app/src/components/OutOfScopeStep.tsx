import type { ScopeAssessment } from '../domain/scope'

export function OutOfScopeStep({ assessment, onEdit, onDelete }: { assessment: ScopeAssessment; onEdit: () => void; onDelete: () => Promise<void> }) {
  return <section className="page narrow-page scope-page"><div className="scope-mark">—</div><div className="eyebrow">Outside the prototype scope</div><h1>Tuntiva should not prepare this case.</h1><p className="lede">The current prototype is limited to ordinary personal or household purchases by consumers in Malaysia. It will not force this case into a route that may not apply.</p><div className="scope-reasons"><strong>Why the workflow stopped</strong><ul>{assessment.reasons.map((reason) => <li key={reason}>{reason}</li>)}</ul></div><div className="notice"><div><span className="notice-mark">i</span><div><h2>What you can do</h2><p>Review the details in case the purpose, location, or category was entered incorrectly. Otherwise, use an authoritative source or qualified adviser for the relevant sector. Tuntiva has not collected evidence for this case.</p></div></div></div><div className="actions split"><button className="secondary" onClick={() => void onDelete()}>Delete draft</button><button className="primary" onClick={onEdit}>Review case details <span>→</span></button></div></section>
}
